# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - OAuth Redirect Port Mismatch
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the OAuth redirect port mismatch bug
  - **Manual Testing Approach**: This is a configuration bug that requires manual verification
  - Test implementation details from Bug Condition in design:
    - Start the vendor portal on port 3000 using `next dev` in `packages/vendor/`
    - Verify the vendor portal is accessible at `http://localhost:3000`
    - Read `packages/backend/.env` and verify `FRONTEND_URL=http://localhost:3001` (incorrect value)
    - Start the backend server on port 5000
    - Initiate Google OAuth login from the vendor portal
    - Complete the Google authentication flow
    - Observe the backend redirect URL in browser network tab or backend logs
    - **EXPECTED OUTCOME**: Backend redirects to `http://localhost:3001/dashboard?token=...&refresh_token=...`
    - **EXPECTED OUTCOME**: Browser displays `ERR_CONNECTION_REFUSED` because nothing is listening on port 3001
    - Document the exact redirect URL and error message as counterexamples
  - The test assertions should match the Expected Behavior Properties from design:
    - After fix: redirect URL should contain `localhost:3000` (not `localhost:3001`)
    - After fix: browser should successfully reach the dashboard page
  - Run test on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (this is correct - it proves the bug exists)
  - Document counterexamples found to understand root cause
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 1.1, 1.2, 1.3_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Other Port Configurations Unchanged
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-buggy inputs (other portals and services)
  - Write manual test cases capturing observed behavior patterns from Preservation Requirements:
    - **User Portal OAuth Flow (Port 3003)**:
      - Start user portal with `next dev -p 3003` in `packages/user/`
      - Verify user portal is accessible at `http://localhost:3003`
      - Initiate Google OAuth login from user portal
      - Complete authentication flow
      - Observe: OAuth callback should redirect to port 3003 (if user portal has its own OAuth config)
      - Document the observed redirect behavior
    - **Admin Portal OAuth Flow (Port 3002)**:
      - Start admin portal with `next dev -p 3002` in `packages/admin/`
      - Verify admin portal is accessible at `http://localhost:3002`
      - Initiate Google OAuth login from admin portal
      - Complete authentication flow
      - Observe: OAuth callback should redirect to port 3002 (if admin portal has its own OAuth config)
      - Document the observed redirect behavior
    - **Backend API Port (Port 5000)**:
      - Verify backend API is running on port 5000
      - Make a test API call to `http://localhost:5000/api/v1/health` or similar endpoint
      - Observe: API responds successfully on port 5000
    - **Agentic Event Orchestrator (Port 8000)**:
      - Verify orchestrator is running on port 8000
      - Make a test API call to `http://localhost:8000/health` or similar endpoint
      - Observe: Orchestrator responds successfully on port 8000
    - **CORS Configuration**:
      - Read `packages/backend/.env` and verify `CORS_ORIGINS` includes all necessary ports
      - Observe: CORS_ORIGINS should include `http://localhost:3000`, `http://localhost:3001`, `http://localhost:3002`, `http://localhost:3003`
    - **Google OAuth Callback URI**:
      - Read `packages/backend/.env` and verify `GOOGLE_REDIRECT_URI`
      - Observe: Should be `http://localhost:5000/api/v1/auth/google/callback`
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7_

- [x] 3. Fix OAuth redirect port configuration

  - [x] 3.1 Update FRONTEND_URL in backend .env file
    - Open `packages/backend/.env`
    - Locate the line `FRONTEND_URL=http://localhost:3001`
    - Change the value to `FRONTEND_URL=http://localhost:3000`
    - Add a comment above the line explaining: `# Frontend vendor portal URL - must match the port where packages/vendor runs (default: 3000)`
    - Save the file
    - _Bug_Condition: isBugCondition(input) where input.isSuccessfulOAuthCallback == true AND settings.frontend_url == "http://localhost:3001" AND actualFrontendPort == 3000_
    - _Expected_Behavior: OAuth callbacks redirect to http://localhost:3000/dashboard with valid JWT tokens, ensuring users can access the application_
    - _Preservation: User portal (3003), admin portal (3002), backend API (5000), orchestrator (8000), CORS origins, and Google OAuth callback URI remain unchanged_
    - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2, 2.3, 3.1, 3.2, 3.3, 3.4, 3.5, 3.6_

  - [x] 3.2 Verify frontend vendor portal port configuration
    - Open `packages/vendor/package.json`
    - Locate the `"dev"` script in the `"scripts"` section
    - Verify it is set to `"next dev"` (no explicit port, defaults to 3000)
    - If an explicit port is set (e.g., `"next dev -p 3001"`), change it to `"next dev"` or `"next dev -p 3000"`
    - Save the file if changes were made
    - _Requirements: 2.2_

  - [x] 3.3 Verify other port configurations are correct
    - Open `packages/backend/.env`
    - Verify `PORT=3001` is set correctly (this should actually be 5000 for the backend API - verify and correct if needed)
    - Verify `CORS_ORIGINS` includes `"http://localhost:3000"` in the array
    - Verify `GOOGLE_REDIRECT_URI=http://localhost:5000/api/v1/auth/google/callback` is correct
    - Open `packages/user/package.json` and verify the dev script uses `"next dev -p 3003"`
    - Open `packages/admin/package.json` and verify the dev script uses `"next dev -p 3002"`
    - Document any discrepancies found
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6_

  - [x] 3.4 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - OAuth Redirect to Correct Port
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Restart the backend server to load the new `FRONTEND_URL` value
    - Ensure the vendor portal is running on port 3000
    - Initiate Google OAuth login from the vendor portal
    - Complete the Google authentication flow
    - Observe the backend redirect URL
    - **EXPECTED OUTCOME**: Backend redirects to `http://localhost:3000/dashboard?token=...&refresh_token=...`
    - **EXPECTED OUTCOME**: Browser successfully loads the dashboard page (no connection refused error)
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - _Requirements: 2.1, 2.2, 2.3_

  - [x] 3.5 Verify preservation tests still pass
    - **Property 2: Preservation** - Other Port Configurations Unchanged
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run all preservation test cases from task 2:
      - User portal OAuth flow on port 3003 (if applicable)
      - Admin portal OAuth flow on port 3002 (if applicable)
      - Backend API on port 5000
      - Agentic event orchestrator on port 8000
      - CORS configuration includes all necessary ports
      - Google OAuth callback URI is unchanged
    - **EXPECTED OUTCOME**: All tests PASS (confirms no regressions)
    - Confirm all tests still pass after fix (no regressions)
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7_

- [x] 4. Checkpoint - Ensure all tests pass
  - Verify the bug condition exploration test passes (OAuth redirects to correct port 3000)
  - Verify all preservation tests pass (other portals and services unchanged)
  - Test the full OAuth flow end-to-end:
    - Start vendor portal on port 3000
    - Start backend on port 5000
    - Complete Google OAuth login
    - Verify successful redirect to dashboard with valid tokens
    - Verify user can access protected resources
  - If any issues arise, document them and ask the user for guidance
  - Mark complete when all tests pass and OAuth flow works correctly
